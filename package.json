{
  "name": "@kolpax/exec-stream",
  "version": "1.0.0",
  "description": "Spawn a child process and expose stdin and stdout as a single duplex stream.",
  "main": "index.js",
  "author": "Martin Daniel Kolpak <martin@kolpax.io>",
  "license": "WTFPL",
  "scripts": {
    "build": "babel src/exec-stream.js --presets es2015 --out-file $npm_package_main"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0"
  }
}
